/*
    For education purposes only.
    (M) -> Macro Constant
*/

#include <iostream>
/* cin, cout */

#include <cstring>
/* strlen, strcpy, strncpy, strstr, strcmp, strncmp */

#include <cctype>
/* tolower, toupper, isalnum, isalpha, isdigit, isupper, islower, ...*/

#include <cstdlib>
/* size_t, malloc, free, exit, EXIT_SUCCESS(M), EXIT_FAILURE(M), rand, srand, bsearch, qsort,
    RAND_MAX(M), NULL(M), abs, div */

#include <string>
/* std::string */

#include <vector>
/* std::vector */

#include <memory>
/* std::unique_ptr, std::shared_ptr, std::weak_ptr */

#include <new>
/* std::bad_alloc */

